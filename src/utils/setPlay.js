export const makeAnswers = (type, answers) => {
  // make answer elements depending on the question type
  if(type === 'multipleChoice') {
    // map through answers and create 4 elements

  } else {
    // map through answers and create 2 elements
  }
  //
};
